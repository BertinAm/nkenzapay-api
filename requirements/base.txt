# Core framework
Django==5.1.6
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3
django-environ==0.11.2
# Serves collected static files from the app process. Passenger hands every
# request to Django, so nothing else is going to serve them.
whitenoise==6.12.0

# Auth and security
argon2-cffi==23.1.0
# Seals uploaded files before they touch the disk. See nkenzapay/common/crypto.py.
cryptography==50.0.1
django-otp==1.5.4
qrcode==8.0
django-ratelimit==4.1.0

# Realtime
channels==4.2.0
daphne==4.1.2

# Background work
celery==5.4.0
redis==5.2.1

# Content, receipts, exports
bleach==6.2.0
openpyxl==3.1.5
reportlab==4.2.5
Pillow==11.0.0

# Outbound HTTP (FX provider)
requests==2.32.3
